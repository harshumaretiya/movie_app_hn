<!DOCTYPE html>
<html class="h-full bg-white">
  <head>
    <title>Movie App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.7.0/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.7.0/dist/js/bootstrap.min.js"></script>
    
    <!-- Add jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>

  <body class="h-full">
    <%= render 'devise/shared/navbar' %>
    <div >
      <% flash.each do |type, msg| %>
        <div class="<%= flash_class(type) %> px-6 py-4 border-0 rounded relative mb-4 bg-<%= color(type) %>-300">
        <span class="inline-block align-middle mr-8">

            <%= msg %>
          </span>
          <button class="absolute bg-transparent text-2xl font-semibold leading-none right-0 top-0 mt-4 mr-6 outline-none focus:outline-none" onclick="this.parentElement.remove();">
            <span>×</span>
          </button>
        </div>
      <% end %>
    </div>
    <main>
      <%= yield %>
    </main>

    <footer>
      <!-- Footer content here -->
    </footer>
  </body>
</html>

<script>
  // Close the alert after 3 seconds (3000 ms)
  window.setTimeout(function() {
    document.querySelector('.alert_dash').style.display = 'none';
  }, 3000);
</script>



